import { JoiValidationExceptionFilter } from './joi-validation-exception.filter';

describe('JoiValidationExceptionFilter', () => {
  it('should be defined', () => {
    expect(new JoiValidationExceptionFilter()).toBeDefined();
  });
});
